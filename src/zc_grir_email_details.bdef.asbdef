projection;
strict ( 2 );
use draft;
define behavior for ZC_GRIR_Email_Details //alias <alias_name>
{
  use create;
  use update;
  use delete;
  use action Activate;
  use action Edit;
  use action Discard;
  use action Resume;
  use action Prepare;

}